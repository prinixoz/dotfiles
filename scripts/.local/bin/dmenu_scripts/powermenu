CHOSEN=$(printf "Lock\nSuspend\nReboot\nShutdown\nLog Out" | dmenu -l 10)

case "$CHOSEN" in
	"Lock") lockscreen ;;
	"Suspend") systemctl suspend;;
	"Reboot") reboot ;;
	"Shutdown") poweroff ;;
	"Log Out") i3-msg exit ;;
	*) exit 1 ;;
esac
